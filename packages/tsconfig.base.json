{
  "compilerOptions": {
    /* Visit https://aka.ms/tsconfig to read more about this file */

    /* Modules */
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "resolveJsonModule": true,
    "rootDir": "${configDir}/src",
    /* Test files are excluded below, so the production package builds
       must not require test-runner types — vitest is a devDependency
       and is absent on deploy hosts that prune dev installs. */
    "types": ["node"],

    /* Emit */
    "declaration": true,
    "declarationMap": true,
    "outDir": "${configDir}/build",
    "sourceMap": true,

    /* JavaScript Support */
    "allowJs": true,

    /* Interop Constraints */
    "forceConsistentCasingInFileNames": true,
    "verbatimModuleSyntax": true,

    /* Projects */
    "composite": true,

    /* Completeness */
    "skipLibCheck": true
  },

  /* All packages use `src` as root directory. Must manually include
     json imports when extending from this config. */
  "include": ["${configDir}/src", "${configDir}/src/**/*.json"],

  /* Never compile test files */
  "exclude": ["${configDir}/src/**/*.test.ts"]
}
